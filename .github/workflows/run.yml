name: Bot H24
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas requests
      - run: python bot_auto.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.8838152453:AAEI37Kh-0BEurFlVSnbQEVOalqW21f1ljQ
 }}
          TELEGRAM_CHAT_ID: ${{ secrets.8838152453:AAEI37Kh-0BEurFlVSnbQEVOalqW21f1ljQ
 }}